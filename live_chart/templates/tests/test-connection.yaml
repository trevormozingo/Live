apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "live.fullname" . }}-test-connection"
  labels:
    {{- include "live.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "live.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "live.fullname" . }}
  labels:
    {{- include "live.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: targetPort
      protocol: TCP
      name: mongodb
  selector:
    {{- include "live.selectorLabels" . | nindent 4 }}
